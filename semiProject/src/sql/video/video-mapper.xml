<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<entry key="selectListCount">
		SELECT COUNT(*) COUNT
		FROM BOARD
		WHERE STATUS = 'Y'
		AND BOARD_TYPE = 1
	</entry>
	<entry key="selectList">
		
	SELECT BOARD_NO "BOARD_NO",USERNO "BOARD_WRITER",BOARD_TYPE,BOARD_TITLE,CREATE_DATE,COUNT
	FROM board
	WHERE STATUS='Y'
	ORDER BY CREATE_DATE DESC;
	</entry>
</properties>